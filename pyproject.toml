[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "comfyui-simple-float-slider"
version = "1.0.0"
description = "ComfyUI custom nodes providing styled draggable float slider widgets"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

[project.urls]
Repository = "http://mcnabnas.local:3000/Carasibana/ComfyUI-SimpleFloatSlider"
